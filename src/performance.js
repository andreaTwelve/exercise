class performance {
    constructor(name, amount, seats) {
        this.name = name;
        this.amount = amount;
        this.seats = seats;
    }

    toString() {
        return ` ${this.name}: ${fthis.amount} (${this.seats} seats)\n`;
    }
}